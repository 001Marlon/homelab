; Raises PHP's upload limits to match the panel's own "Upload Limit" setting
; (Admin -> Nodes -> Advanced Settings). Stock php.ini-production defaults
; are only 2M/8M, which silently caps every upload before Laravel sees it.
upload_max_filesize = 256M
post_max_size = 300M
